# Styles

Next.js App Router requires global styles to be imported from `app/layout.tsx`,
so the project's global stylesheet lives at `app/globals.css` (Tailwind base +
custom utility layers: glassmorphism, grid background, gradient text, etc.).

This folder is kept for any additional standalone stylesheets or design-token
documentation you want to add later.
